# URL-Shortener
 For a given URL, generates a short URL of random length between 4 and 6 characters. The database stores the full and short URLs, creation date and the number of hits. After saving in the database, the short link can be visited.

 Stack: ASP.NET Core 8, Razor Pages, Entity Framework Core, MySQL (MariaDB 10.3), JavaScript, HTML, CSS.
